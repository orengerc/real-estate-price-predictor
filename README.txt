file list + descriptions:
task 1 - directory, contains: regression.py (where the whole code lies), requirements.txt (to download before starting), 
			      revenue_model.joblib (binary file which contains the trained revenue prediction model),
			      vote_average_model.joblib (binary file which contains the trained vote-average prediction model)
project.pdf - desciption of how we did everything, answers to the questions. 
USERS.txt - all usernames and ids of the group members.